export type Patient = {
    id: number;
    firstName: string;
    lastName: string;
    updatedAt: string;
}

export type Config = {
    ***REMOVED***: string;
    value: string;
    updatedAt: string;
}